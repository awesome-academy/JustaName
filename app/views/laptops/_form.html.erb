<%= form_for(@laptop) do |f| %>
    <% if @laptop.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@laptop.errors.count, "error") %> prohibited this laptop from being saved:</h2>
            <ul>
            <% @laptop.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
            </ul>
        </div>
    <% end %>
        <div class="formBx">
            <%= f.label "Title" %><br>
            <%= f.text_field :title , class: "formIn" %><br>
        </div>

        <div class="formBx">
            <%= f.label "Brand" %><br>
            <%= f.select :brand, options_for_select(Laptop.brands.map { |k, v| [k, v] }) %>
        </div>

        <div class="formBx">
            <%= f.label "Price" %><br>
            <%= f.number_field :price , class: "formIn" %><br>
        </div>

        <div class="formBx">
            <%= f.label "Description" %><br>
            <%= f.text_area :description, :rows => 5 , class: "formIn formArea" %><br>
        </div>

        <div class="formBx">
            <%= f.file_field :image %><br>
        </div>

        <%= f.submit "Create", class: "submit" %>
<% end %>